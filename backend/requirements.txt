Django>=6.0,<6.1
djangorestframework>=3.17
django-environ>=0.14
django-unfold>=0.100,<0.101
psycopg2-binary>=2.9
django-cors-headers>=4.9
django-axes>=8,<9
django-csp>=4,<5
drf-spectacular>=0.30
# Pinned explicitly although drf-spectacular already requires it: `spectacular
# --format openapi-yaml` needs it, and so does the deploy-manifest drift guard in
# tests/test_env_surface_drift.py. A direct use deserves a direct pin -- otherwise the
# day drf-spectacular drops the dependency, an unrelated upgrade breaks both.
PyYAML>=6,<7
gunicorn>=23
# Keep pytest below 9 until pytest-django supports its changed fixture finalizers:
# an unbounded upgrade broke every test during setup and looked like a mass regression.
pytest>=8,<9
pytest-django>=4.12
whitenoise>=6.12
segno
djangorestframework-simplejwt>=5.5
cryptography>=44
django-storages[s3]>=1.14,<2
boto3>=1.36,<2
requests>=2.32,<3
httpx[http2]==0.28.1
openpyxl>=3.1,<4
Pillow
reportlab>=4.2,<5
python-dateutil>=2.9,<3
icalendar>=7.0,<8
celery[redis]>=5.4,<6
redis>=5.0,<7
dnspython>=2.7,<3
stripe==15.3.1
