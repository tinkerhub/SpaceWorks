Django>=6.0,<6.1
djangorestframework>=3.17
django-environ>=0.14
django-unfold>=0.100,<0.101
psycopg2-binary>=2.9
django-cors-headers>=4.9
django-axes>=8,<9
django-csp>=4,<5
drf-spectacular>=0.30
# Pinned explicitly although drf-spectacular already requires it: `spectacular
# --format openapi-yaml` needs it, and so does the deploy-manifest drift guard in
# tests/test_env_surface_drift.py. A direct use deserves a direct pin -- otherwise the
# day drf-spectacular drops the dependency, an unrelated upgrade breaks both.
PyYAML>=6,<7
gunicorn>=23
# The <9 cap is LIFTED. It was added when pytest-django 4.12 broke on pytest 9's changed
# fixture finalizers; 4.14 declares `pytest>=7.0.0` with no upper bound and the suite runs
# clean on 9.1.1. Keeping the cap held pytest at 8.4.2, which carries PYSEC-2026-1845 and
# failed the pip-audit gate. Do not reintroduce the cap without also pinning
# pytest-django to the version that actually needs it.
pytest>=9
pytest-django>=4.14
whitenoise>=6.12
segno
djangorestframework-simplejwt>=5.5
cryptography>=44
django-storages[s3]>=1.14,<2
boto3>=1.36,<2
requests>=2.32,<3
httpx[http2]==0.28.1
openpyxl>=3.1,<4
Pillow
reportlab>=4.2,<5
python-dateutil>=2.9,<3
icalendar>=7.0,<8
celery[redis]>=5.4,<6
redis>=5.0,<7
dnspython>=2.7,<3
stripe==15.3.1
